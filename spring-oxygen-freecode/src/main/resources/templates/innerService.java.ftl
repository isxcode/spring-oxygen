package ${packageName};

import org.springframework.stereotype.Service;

/**
 * Interface - ${tableComment!""}
 *
 * @author ${freecodeProperties.author}
 * @since ${freecodeProperties.version}
 */
@Service
public interface ${className?cap_first}InnerService {

}