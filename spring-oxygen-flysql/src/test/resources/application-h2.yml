spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/h2
    username: root
    password: root
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema_h2.sql

oxygen:
  flysql:
    show-log: true
